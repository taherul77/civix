# Auto-translate provider — point at your self-hosted LibreTranslate.
# Default if unset: http://localhost:5000/translate
NEXT_PUBLIC_TRANSLATE_URL=http://localhost:5000/translate

# Optional API key (only if LibreTranslate is configured to require one).
# NEXT_PUBLIC_TRANSLATE_API_KEY=
